# 后台设计规范

本规范约束 `edu-system` 的后台管理界面。目标是让操作人员能够在高密度数据界面中快速筛选、维护和导出高校业务数据。

## 1. 设计基线

- 基础组件固定使用 Ant Design；全局主题通过 `ConfigProvider` 配置，避免在页面中复制颜色、圆角和控件样式。
- 风格为专业、明亮、克制的企业后台：白色导航与内容容器、浅灰蓝工作区、少量品牌蓝作为唯一主要强调色。
- 默认桌面宽度优先；在窄屏时侧栏折叠，表格横向滚动，表单抽屉全宽显示。
- 不使用渐变、装饰性光斑、毛玻璃、过度圆角、营销式 Hero 或大面积插画。

## 2. 布局

- 整体使用 `Layout`：左侧 `Sider`、顶部 `Header`、中间内容区。
- 左栏宽 208px，白色背景、右侧细边线；品牌位于顶部，主导航按业务域分组。
- 顶栏高度 64px，包含折叠按钮、面包屑、全局搜索入口和用户操作区。
- 内容区内边距为 24px。页面标题、说明、统计卡片、业务表格按从上到下的操作顺序组织。
- 内容容器和卡片使用 8px 圆角，边框使用低对比度中性灰，阴影仅用于悬浮层。

## 3. 页面结构

管理端与运营端共享以下页面模板，业务页面按需组合：

1. 应用壳：左侧菜单、顶部面包屑、全局入口与用户操作区。
2. 页面标题区：标题、说明和当前权限或状态。
3. 指标卡：由业务传入标题、数值、图标和语义色，不写入领域名称。
4. 数据工作台：由业务传入搜索、筛选、命令和表格内容，统一容器、间距与响应式规则。
5. 表单抽屉：由业务提供字段与保存动作，使用统一的 Ant Design Drawer 与 Form 交互。
6. 系统设置：一级菜单下按“安全”等能力建立二级分组，具体设置作为三级菜单；使用受限访问的紧凑表单卡，密码和密钥只允许输入，不展示、回填或持久化到浏览器。

共享组件位于 `components/ui/`，不得读取业务数据、发起请求或判断业务权限。高校信息维护与未来运营端分别在自己的领域目录中组合这些组件。

## 4. 视觉 Token

| 用途 | 值 |
| --- | --- |
| 品牌主色 | `#1677ff` |
| 页面背景 | `#f5f7fa` |
| 容器背景 | `#ffffff` |
| 主文字 | `#1f1f1f` |
| 次级文字 | `#667085` |
| 分割线 | `#eaecf0` |
| 成功状态 | `#16a34a` |
| 警示状态 | `#f59e0b` |
| 风险状态 | `#ef4444` |

正文优先使用 `PingFang SC`、`Microsoft YaHei` 与系统无衬线字体。正文为 14px，辅助文字为 12px，紧凑面板标题为 16-20px；不得使用负字距。

## 5. 交互与状态

- 写操作必须在管理员会话解锁后才可执行；未解锁时通过登录弹窗说明原因。
- 管理员会话由管理端布局级 Provider 维护，跨页面切换不得重复创建会话状态；所有受限页面统一使用访问门禁隐藏业务内容。
- 删除单条或多条记录必须二次确认，确认文案应包含影响数量。
- 读取中使用骨架或表格加载态；无数据、筛选无结果和请求失败必须区分展示。
- 成功、失败和权限提示统一用 Ant Design `message` 或 `notification`，不要保留页面内临时状态文案。
- 左侧占位菜单可展示，但点击时只提示“该模块正在规划中”，不得伪造页面跳转或数据。

## 6. 实现约束

- 图标优先使用 `@ant-design/icons`，只在 Ant Design 无对应图标时使用现有图标库。
- CSS 只处理布局和 Ant Design 覆盖无法表达的页面级细节，保持多行规则格式。
- 不在后台页面内引入首页的大屏视觉语言；两个页面共享数据契约，不共享布局假设。
